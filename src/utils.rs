use std::path::*;

pub fn usage() {
    println!("      --help                Print this information");
    println!("      --index [dir]         Index the provided dir");
}

pub fn open_files_in_dir(p: PathBuf) -> Vec<PathBuf> {
    todo!()
    // let paths: Vec<PathBuf>;
    // if p.is_dir() {
    // }
}

